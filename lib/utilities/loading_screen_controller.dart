import 'package:flexyy/models/flexyy_typedefs.dart';

class LoadingScreenController {
  final Update update;
  final Remove remove;
  const LoadingScreenController({required this.update, required this.remove});
}
